using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/** 
 *    @author Matthew Ahearn
 *    @since 1.2.2
 *    @version 0.0.0
 *    
 *    Controller for a block which will try to jump up and block players from being able to get past it. Teamwork will be needed to pass it successfully.
 */
public class JumpingBlock : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
